# ambxst-plymouth
animated plymouth theme for ambxst

1. `sudo pacman -S plymouth`
2. Clone this repo and move the ambxst folder to ` /usr/share/plymouth/themes `
3. `sudo plymouth-set-default-theme -R /usr/share/plymouth/themes/ambxst/ambxst.plymouth `